viewsize 80
gamma 0.8
volume 0.5
bgmvolume 0.5

bind uparrow +forward
bind l +moveleft
bind r +moveright
bind downarrow +back

bind x "impulse 10"

bind c +jump
bind b +speed
bind a +attack

bind leftarrow +left
bind rightarrow +right
bind y +lookup
bind z +lookdown

